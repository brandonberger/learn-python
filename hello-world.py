# priting a line and another line
print "This line will be printed."

# Python uses indentation for blocks, instead of curly braces.
# Both tabs and spaces are supported, but standard Python requires four spaces.
# i.e

x = 1
if x == 1:
	print "x is 1."
